export const environment = {
  production: false,
  apiUrl: 'http://localhost:3000',
  useMockApi: true /** Set to true to use the mock API, false to use the real API. Use dummy data for deployed version while waiting for nodejs backend */,
};
